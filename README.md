# mcp-audit
mcp_audit is a CLI security tool that scans MCP (Model Context Protocol) server configurations and startup directories for pre-initialization injection risks. Inspired by the Gemini CLI RCE vulnerability, it checks config file permissions, detects suspicious pre-sandbox command hooks, environment variable injections, and flags potential supply chai
